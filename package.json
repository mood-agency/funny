{
  "name": "@ironmussa/funny",
  "version": "0.1.1",
  "private": false,
  "description": "Codex App clone powered by Claude Code — parallel agent orchestration with git worktrees",
  "author": "Anthropic",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ironmussa/funny.git"
  },
  "bin": {
    "funny": "./bin/funny.js"
  },
  "files": [
    "bin",
    "packages/server/dist",
    "packages/client/dist"
  ],
  "engines": {
    "bun": ">=1.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "bun ./bin/funny.js",
    "dev": "concurrently --kill-others \"bun run --cwd packages/server dev\" \"bun run --cwd packages/client dev\"",
    "dev:test": "concurrently --kill-others \"bun run --cwd packages/server dev:test\" \"bun run --cwd packages/client dev:test\"",
    "dev:server": "bun run --cwd packages/server dev",
    "dev:client": "bun run --cwd packages/client dev",
    "dev:agent": "bun run --cwd packages/agent dev",
    "build": "bun run --cwd packages/client build && bun run --cwd packages/server build",
    "prepublishOnly": "npm run build",
    "db:push": "bun run --cwd packages/server db:push",
    "db:studio": "bun run --cwd packages/server db:studio",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "build:sidecar": "node scripts/build-sidecar.js",
    "test": "bun run --cwd packages/shared test && bun run --cwd packages/core test && bun run --cwd packages/agent test && bun run --cwd packages/server test && bun run --cwd packages/client test",
    "prepare": "husky",
    "security:check": "npm run security:secrets && npm run security:audit",
    "security:secrets": "npx secretlint \"**/*\"",
    "security:audit": "npx audit-ci --moderate",
    "security:snyk": "npx snyk test --all-projects"
  },
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "^11.3.1",
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/cli": "^2.10.0",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "neverthrow": "^8.2.0",
    "secretlint": "^11.3.1"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.14.1",
    "node-pty": "^1.1.0"
  },
  "optionalDependencies": {
    "playwright": "^1.50.0",
    "@openai/codex-sdk": "^0.101.0"
  },
  "resolutions": {
    "esbuild": ">=0.24.3"
  }
}

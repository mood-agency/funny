# Pipeline Agent Service Configuration
# Copy this file to .env and adjust values as needed.

# Port for the pipeline agent HTTP service (default: 3002)
PORT=3002

# URL of the main server's ingest webhook endpoint.
# Pipeline events are forwarded here so they appear in the UI.
INGEST_WEBHOOK_URL=http://localhost:3001/api/ingest/webhook

# Optional shared secret for authenticating webhook deliveries.
# Must match INGEST_WEBHOOK_SECRET on the server if set.
# INGEST_WEBHOOK_SECRET=

# Path to the project root (default: current working directory)
# PROJECT_PATH=/path/to/your/project

# Abbacchio log server URL (default: http://localhost:4000/api/logs)
# ABBACCHIO_URL=http://localhost:4000/api/logs

# Abbacchio channel name (default: funny-agent)
# ABBACCHIO_CHANNEL=funny-agent

{
  "name": "@funny/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun run build.ts --watch",
    "package": "bun run build.ts && bun run package.ts"
  },
  "dependencies": {
    "@funny/funny-client": "workspace:*",
    "@funny/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "esbuild": "^0.24.0"
  }
}
